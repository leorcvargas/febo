import styled from '../../styled-components';

export const PlayerActionsRow = styled.div`
  display: flex;
  justify-content: space-evenly;
  margin-top: 25px;
`;

export const PlayerButton = styled.button`
  border: none;
  outline: none;
  background: none;
  cursor: pointer;
`;
